apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment
  template:
    metadata:
      labels:
        app: payment
    spec:
      containers:
      - name: payment
        image: 643327307650.dkr.ecr.us-east-1.amazonaws.com/k8s-services/repo/payment-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 5001
